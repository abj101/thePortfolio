import type { MemorySceneProps } from "../stack/types";

/** Memory 1 slot — R3F content ships later; chrome lives in stack overlay. */
export default function ProofScene(_props: MemorySceneProps) {
  return null;
}
